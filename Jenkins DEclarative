pipeline{
    agent any
    stages{
        stage("gitcheckout"){
            steps{
               git 'https://github.com/prawin429/maven.git'
            }
        }
        stage("maven build"){
            steps{
                sh "mvn clean package"
            }
        }
    }
}
